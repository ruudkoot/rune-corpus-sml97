# Rune e840204151663baf1139c8096b566301e9ced37d

The full commit hash above is this compiler's corpus version. Use `stage1.record`
with installed Rune as its recorded external seed, then `stage2.record` with
this commit's validated stage-1 artifact. See [the shared Rune instructions](../README.md)
for the bootstrap stages, compiler/VM pairing, validation and manual commands.
